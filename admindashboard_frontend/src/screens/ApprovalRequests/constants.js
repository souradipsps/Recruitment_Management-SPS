import { T } from "../../theme";

// Shared label style used across modal fields.
export const labelCss = {
  fontSize: 10, fontWeight: 700, color: T.inkFaint, textTransform: "uppercase",
  letterSpacing: "0.06em", marginBottom: 4,
};
